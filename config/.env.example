# Exemplo de .env usado apenas para referência local.
# NÃO adicionar chaves privadas ou segredos neste arquivo no repositório.
# Em produção (Cloud Run) use Secret Manager e variáveis de ambiente do CI.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_STORAGE_BUCKET=
VITE_BACKEND_URL=
